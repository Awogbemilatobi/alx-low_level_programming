#include<stdio.h>
/**
 * _isdigit - checks for digit
 *
 * Description: checks for digit zero through 9
 * Return: returns 1 success
*/
int _isdigit(int c);
{
	int c;

	if (c>=0 && c<=9)
	{
		return (1);
	}

	else
	{
		return (0)
	}
}	
